Stage all changes, commit with the message "Update website", and push to origin main.

Steps:
1. Run `git add .`
2. Run `git commit -m "Update website"`
3. Run `git push`
4. Confirm the push was successful and that Netlify will now redeploy the live site.
